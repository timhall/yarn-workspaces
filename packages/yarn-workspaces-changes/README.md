# yarn-workspaces-changes

Workspace-aware change detection compared to the specified git reference.

```
$ workspaces-changes master

# package-a package-c
```

### changes(options?)

### workspaces-changes <reference>

- `--verbose` Include detailed workspace information
